<?xml version="1.0" encoding="ISO-8859-1"?>
<project basedir="." default="compile" name="The File Plugin For FenixFramework">
	
	<property name="src.dir" location="src" />
	<property name="build.dir" location="build" />
	<property name="dist.dir" location="dist" />
	<property name="lib.dir" location="lib" />
	<property name="conf.dir" location="conf"/>
	<property name="jar.filename" value="${dist.dir}/fenix-framework-file-plugin" />
	<property name="src.gen" location="src_gen" />

	<taskdef resource="pt/ist/fenixframework/dml-ant.xml">
		<classpath>
	 			<fileset dir="${lib.dir}" includes="*.jar"/>
		</classpath>
	</taskdef>
	
	<path id="class.path.ref">
		<fileset dir="${lib.dir}">
			<include name="*.jar" />
		</fileset>
		<fileset dir="${dist.dir}">
			<include name="*.jar" />
		</fileset>
	</path>

	<target name="clean-all" description="Removes any generated files">
		<delete dir="${build.dir}" />
		<delete dir="${dist.dir}" />
		<delete dir="${src.gen}" />
	</target>

	<target name="compile" depends="generate-domain" description="Compiles the plugin">
		<mkdir dir="${build.dir}" />
		<javac destdir="${build.dir}" extdirs="${lib.dir}" target="1.5" debug="on"> 
			<src path="${src.dir}" />
			<src path="${src.gen}" />
		</javac>
		<antcall target="post-compile"/>
	</target>

	<target name="post-compile" description="Bytecode Magic to inject constructors">
		<dml-post-processor dir="${build.dir}">
			<fileset dir="${conf.dir}"/>
			<classpath>
				<fileset dir="${lib.dir}" includes="*.jar" />
				<dirset dir="${build.dir}" />
			</classpath>
		</dml-post-processor>
	</target>
	
	<target name="jar" depends="generate-domain,compile" description="Generates the jar with the plugin">
		<copy file="${conf.dir}/file-plugin.dml" todir="${build.dir}" />
		<mkdir dir="${dist.dir}" />
		<jar destfile="${jar.filename}.jar" basedir="${build.dir}" />
	</target>

	<target name="generate-domain">
		<dml-domain-generator destDirectory="${src.dir}" destDirectoryBase="${src.gen}">
			<fileset dir="${conf.dir}">
				<include name="*.dml" />
			</fileset>
		</dml-domain-generator>
	</target>
</project>
